SwiftUI is Apple's **declarative** UI framework, introduced at WWDC 2019.

It works by describing *what* the UI should look like for a given state, rather than imperatively mutating views. Read more in the [official docs](https://developer.apple.com/swiftui).

The core building block is the `View` protocol — every screen, every button, every label conforms to it.
